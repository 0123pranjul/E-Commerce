@inject JobPortalManagement.Services.Interface.IMenuService MenuService
@{
    var roleId = Context.Session.GetString("RoleId");
    List<JobPortalManagement.Models.DTO.MenuItemViewModel> menus = new List<JobPortalManagement.Models.DTO.MenuItemViewModel>();
    if (!string.IsNullOrEmpty(roleId))
    {
        menus = await MenuService.GetMenusByRoleAsync(roleId);
    }
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - JobPortal Pro</title>

    <!-- Core Stylesheets -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" integrity="sha384-tuP9c7RNXtP2JfJfQb1Yc4BHk2VouH1aMBNJ4V7yB4i1Chmk/3L6XJFAvC4jADeR" crossorigin="anonymous" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link href="~/css/layout.css" rel="stylesheet" />
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />

   
</head>
<body>
    <div class="layout-wrapper">
        <!-- Modern Sidebar -->
        <nav class="sidebar" id="sidebar">
            <!-- Sidebar Header -->
            <div class="sidebar-header">
                <button class="sidebar-toggle-btn" onclick="toggleSidebarCollapse()">
                    <i class="fas fa-bars"></i>
                </button>
                <div>
                    <h3>Job Portal</h3>
                    <h6>Management System</h6>
                </div>
            </div>

            <!-- Sidebar Menu -->
            <div class="sidebar-menu">
                @if (menus.Any())
                {
                    <div class="menu-section-title">Navigation</div>
                    <!-- Recursive Partial View for Menus -->
                    @await Html.PartialAsync("_MenuItems", menus)
                }
                else
                {
                    <div class="menu-section-title">Quick Links</div>
                    <div class="menu-item">
                        <a class="menu-link" asp-controller="Home" asp-action="Index">
                            <i class="fas fa-home"></i>
                            <span>Dashboard</span>
                        </a>
                    </div>
                    <div class="menu-item">
                        <a class="menu-link" asp-controller="Account" asp-action="Login">
                            <i class="fas fa-sign-in-alt"></i>
                            <span>Login</span>
                        </a>
                    </div>
                    <div class="menu-item">
                        <a class="menu-link" asp-controller="Registration" asp-action="Index">
                            <i class="fas fa-user-plus"></i>
                            <span>Register</span>
                        </a>
                    </div>
                }
            </div>
        </nav>

        <!-- Main Content Area -->
        <div class="main-content" id="main-content">
            <!-- Modern Header -->
            <header class="top-header">
                <div class="header-content">
                    <div class="header-left">
                        <button class="sidebar-toggle d-md-none" onclick="toggleSidebarMobile()">
                            <i class="fas fa-bars"></i>
                        </button>
                        <nav class="breadcrumb-modern">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="/">Home</a></li>
                                <li class="breadcrumb-item active">@ViewData["Title"]</li>
                            </ol>
                        </nav>
                    </div>

                    <div class="header-right">
                        <div class="header-actions">
                            <button class="header-btn" title="Notifications">
                                <i class="fas fa-bell"></i>
                                <span class="notification-badge"></span>
                            </button>
                            <button class="header-btn" title="Messages">
                                <i class="fas fa-envelope"></i>
                            </button>
                            <button class="header-btn" title="Settings">
                                <i class="fas fa-cog"></i>
                            </button>
                        </div>

                        <div class="user-profile" onclick="toggleUserMenu()">
                            <div class="user-avatar">
                                <i class="fas fa-user"></i>
                            </div>
                            <div class="user-info">
                                <div class="user-name">Admin User</div>
                                <div class="user-role">System Administrator</div>
                            </div>
                            <i class="fas fa-chevron-down" style="font-size: 0.75rem; color: rgba(255, 255, 255, 0.8);"></i>
                        </div>
                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="logout-btn">Logout</button>
                        </form>
                    </div>
                </div>
            </header>

            <!-- Page Content -->
            <main class="content-area fade-in">
                @RenderBody()
            </main>

            <!-- Modern Footer -->
            <footer class="modern-footer">
                <div class="container">
                    <div class="footer-content">
                        <div class="row">
                            <div class="col-lg-4 col-md-6 mb-4">
                                <div class="footer-section">
                                    <h6>JobPortal Pro</h6>
                                    <p class="text-white mb-3" style="opacity: 0.8;">Your comprehensive job management solution. Connecting talent with opportunities through innovative technology.</p>
                                    <div class="social-links">
                                        <a href="#" class="social-link" title="Facebook">
                                            <i class="fab fa-facebook-f"></i>
                                        </a>
                                        <a href="#" class="social-link" title="Twitter">
                                            <i class="fab fa-twitter"></i>
                                        </a>
                                        <a href="#" class="social-link" title="LinkedIn">
                                            <i class="fab fa-linkedin-in"></i>
                                        </a>
                                        <a href="#" class="social-link" title="Instagram">
                                            <i class="fab fa-instagram"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                            <div class="col-lg-2 col-md-6 mb-4">
                                <div class="footer-section">
                                    <h6>Quick Links</h6>
                                    <ul class="footer-links">
                                        <li><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
                                        <li><a href="#">Jobs</a></li>
                                        <li><a href="#">Candidates</a></li>
                                        <li><a href="#">Companies</a></li>
                                        <li><a href="#">Reports</a></li>
                                    </ul>
                                </div>
                            </div>
                            <div class="col-lg-2 col-md-6 mb-4">
                                <div class="footer-section">
                                    <h6>Support</h6>
                                    <ul class="footer-links">
                                        <li><a href="#">Help Center</a></li>
                                        <li><a href="#">Documentation</a></li>
                                        <li><a href="#">Contact Us</a></li>
                                        <li><a href="#">FAQ</a></li>
                                        <li><a href="#">Community</a></li>
                                    </ul>
                                </div>
                            </div>
                            <div class="col-lg-2 col-md-6 mb-4">
                                <div class="footer-section">
                                    <h6>Legal</h6>
                                    <ul class="footer-links">
                                        <li><a asp-controller="Home" asp-action="Privacy">Privacy Policy</a></li>
                                        <li><a href="#">Terms of Service</a></li>
                                        <li><a href="#">Cookie Policy</a></li>
                                        <li><a href="#">GDPR</a></li>
                                        <li><a href="#">Compliance</a></li>
                                    </ul>
                                </div>
                            </div>
                            <div class="col-lg-2 col-md-6 mb-4">
                                <div class="footer-section">
                                    <h6>Company</h6>
                                    <ul class="footer-links">
                                        <li><a href="#">About Us</a></li>
                                        <li><a href="#">Careers</a></li>
                                        <li><a href="#">News</a></li>
                                        <li><a href="#">Blog</a></li>
                                        <li><a href="#">Partners</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="footer-bottom">
                        <div class="row align-items-center">
                            <div class="col-md-6 text-md-start text-center">
                                <p>&copy; 2025 JobPortal Pro. All rights reserved.</p>
                            </div>
                            <div class="col-md-6 text-md-end text-center">
                                <p>Made with <i class="fas fa-heart text-danger"></i> by <a href="#">Your Company</a></p>
                            </div>
                        </div>
                    </div>
                </div>
            </footer>
        </div>
    </div>

    <!-- Core Scripts -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <script>
        // Sidebar collapse toggle for desktop and mobile
        function toggleSidebarCollapse() {
            const sidebar = document.getElementById('sidebar');
            const mainContent = document.getElementById('main-content');
            sidebar.classList.toggle('collapsed');
            mainContent.classList.toggle('collapsed');
        }

        // Sidebar toggle for mobile (full show/hide)
        function toggleSidebarMobile() {
            const sidebar = document.getElementById('sidebar');
            if (!sidebar.classList.contains('collapsed')) {
                sidebar.classList.toggle('active');
            }
        }

        // Menu toggle animation
        function toggleMenu(element) {
            const toggle = element.querySelector('.menu-toggle');
            toggle.classList.toggle('rotated');
        }

        // User menu toggle (placeholder)
        function toggleUserMenu() {
            console.log('User menu clicked');
        }

        // Active menu highlighting
        $(document).ready(function () {
            // Highlight active menu item
            const currentPath = window.location.pathname;
            $('.menu-link, .submenu-link').each(function () {
                const href = $(this).attr('href');
                if (href && href !== '#' && href === currentPath) {
                    $(this).addClass('active');
                    // Expand all parent menus
                    let parentCollapse = $(this).closest('.collapse');
                    while (parentCollapse.length) {
                        parentCollapse.addClass('show');
                        const parentToggle = $(`[data-bs-target="#${parentCollapse.attr('id')}"] .menu-toggle`);
                        parentToggle.addClass('rotated');
                        parentCollapse = parentCollapse.closest('.menu-item').closest('.collapse');
                    }
                }
            });

            // Handle collapse events
            $('.collapse').on('show.bs.collapse', function () {
                $(this).addClass('slide-down');
            });

            $('.collapse').on('hide.bs.collapse', function () {
                $(this).removeClass('slide-down');
            });

            // Close sidebar when clicking outside on mobile
            $(document).click(function (event) {
                if ($(window).width() <= 768) {
                    const sidebar = $('#sidebar');
                    const sidebarToggle = $('.sidebar-toggle');
                    const sidebarToggleBtn = $('.sidebar-toggle-btn');

                    if (!sidebar.is(event.target) && sidebar.has(event.target).length === 0 &&
                        !sidebarToggle.is(event.target) && sidebarToggle.has(event.target).length === 0 &&
                        !sidebarToggleBtn.is(event.target) && sidebarToggleBtn.has(event.target).length === 0) {
                        sidebar.removeClass('active');
                    }
                }
            });

            // Handle window resize
            $(window).resize(function () {
                if ($(window).width() > 768) {
                    $('#sidebar').removeClass('active');
                }
            });

            // Smooth scrolling for valid anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                const href = anchor.getAttribute('href');
                if (href && href !== '#') {
                    anchor.addEventListener('click', function (e) {
                        e.preventDefault();
                        const target = document.querySelector(href);
                        if (target) {
                            target.scrollIntoView({
                                behavior: 'smooth',
                                block: 'start'
                            });
                        }
                    });
                }
            });
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>